/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./js/main.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./js/main.js":
/*!********************!*\
  !*** ./js/main.js ***!
  \********************/
/*! no static exports found */
/***/ (function(module, exports) {

var pageBody = document.querySelector('.page__body');
var buttonHeader = document.querySelector('.page-header__button');
var modalForm = document.querySelector('.modal');
var modalInput = document.querySelector('.modal [type="text"]');
var modalOverlay = document.querySelector('.modal__overlay');
var buttonClose = document.querySelector('.modal__button');
var accordionsFooter = document.querySelectorAll('.page-footer__accordion');
var textAll = document.querySelector('.about__text--all');
var buttonAll = document.querySelector('.about__link');
var maskedInputs = document.querySelectorAll('[data-mask]');
var isElements = accordionsFooter && textAll && buttonAll && buttonHeader && modalForm;

var activateScript = function activateScript() {
  accordionsFooter.forEach(function (item) {
    item.classList.remove('page-footer__accordion--nojs');
    item.classList.add('page-footer__accordion--closed');
  });
};

var disableScript = function disableScript() {
  accordionsFooter.forEach(function (item) {
    item.classList.add('page-footer__accordion--nojs');
  });
};

var onClick = function onClick(element, functionName) {
  element.addEventListener('click', function () {
    functionName();
  });
};

var openModal = function openModal() {
  if (!modalForm.classList.contains('modal--opened')) {
    modalForm.classList.add('modal--opened');
    pageBody.classList.add('page__body--hidden');
    modalInput.focus();
    disableFocus();
    document.addEventListener('keydown', onDocumentKeydown);
  }
};

var disableFocus = function disableFocus() {
  document.addEventListener('focus', function (evt) {
    if (modalForm && !modalForm.contains(evt.target)) {
      evt.stopPropagation();
      modalInput.focus();
    }
  }, true);
};

var closeModal = function closeModal() {
  if (modalForm.classList.contains('modal--opened')) {
    modalForm.classList.remove('modal--opened');
    pageBody.classList.remove('page__body--hidden');
    document.removeEventListener('keydown', onDocumentKeydown);
  }
};

var onDocumentKeydown = function onDocumentKeydown(evt) {
  if (evt.keyCode === 27) {
    evt.preventDefault();
    closeModal();
  }
};

var setModalActions = function setModalActions() {
  onClick(buttonClose, closeModal);
  onClick(modalOverlay, closeModal);
  onClick(buttonHeader, openModal);
};

var onClickAccordion = function onClickAccordion(element) {
  element.addEventListener('click', function () {
    if (element.classList.contains('page-footer__accordion--closed')) {
      accordionsFooter.forEach(function (item) {
        item.classList.add('page-footer__accordion--closed');
      });
      element.classList.remove('page-footer__accordion--closed');
      element.classList.add('page-footer__accordion--opened');
    } else {
      element.classList.remove('page-footer__accordion--opened');
      element.classList.add('page-footer__accordion--closed');
    }
  });
};

var selectAccordions = function selectAccordions() {
  accordionsFooter.forEach(function (accordion) {
    onClickAccordion(accordion);
  });
};

var activateButtonAll = function activateButtonAll() {
  if (textAll.classList.contains('about__text--all-closed')) {
    textAll.classList.remove('about__text--all-closed');
    buttonAll.textContent = 'Свернуть';
  } else {
    textAll.classList.add('about__text--all-closed');
    buttonAll.textContent = 'Подробнее';
  }
};

var onClickButtonAll = function onClickButtonAll() {
  buttonAll.addEventListener('click', function (evt) {
    evt.preventDefault();
    activateButtonAll();
  });
};

var createMask = function createMask() {
  var _loop = function _loop(index) {
    maskedInputs[index].addEventListener('input', function () {
      var inputMask = maskedInputs[index];
      var maskData = inputMask.dataset.mask;
      var valueInput = inputMask.value;
      var literalPattern = /[0\*]/;
      var numberPattern = /[0-9]/;
      var newValue = '';

      try {
        var maskLength = maskData.length;
        var valueIndex = 0;
        var maskIndex = 0;

        for (; maskIndex < maskLength;) {
          if (maskIndex >= valueInput.length) {
            break;
          }

          if (maskData[maskIndex] === '0' && valueInput[valueIndex].match(numberPattern) === null) {
            break;
          }

          while (maskData[maskIndex].match(literalPattern) === null) {
            if (valueInput[valueIndex] === maskData[maskIndex]) {
              break;
            }

            newValue += maskData[maskIndex++];
          }

          newValue += valueInput[valueIndex++];
          maskIndex++;
        }

        inputMask.value = newValue;
      } catch (evt) {
        return;
      }
    });
  };

  for (var index = 0; index < maskedInputs.length; index++) {
    _loop(index);
  }
};

if (isElements) {
  activateScript();
  selectAccordions();
  onClickButtonAll();
  setModalActions();
  createMask();
} else {
  disableScript();
}

/***/ })

/******/ });
//# sourceMappingURL=main.min.js.map